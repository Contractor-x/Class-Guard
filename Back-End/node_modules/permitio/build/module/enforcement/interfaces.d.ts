import { Dict } from '../utils/dict';
export interface IUser {
    key: string;
    firstName?: string;
    lastName?: string;
    email?: string;
    attributes?: Dict;
}
export declare type IAction = string;
export interface IResource {
    type: string;
    key?: string;
    tenant?: string;
    attributes?: Dict;
}
export interface PolicyDecision {
    allow: boolean;
}
export interface OpaDecisionResult {
    result: PolicyDecision;
}
